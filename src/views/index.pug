extends layout

block content
  h1= title
  p Welcome to #{title}

  ul
    for book in books
      li= book.title


  .clear-fix
  if page > 1
    a(href='/?page='+(page -1)) Previous
  a.pull-right(href='/?page='+(page +1)) Next
